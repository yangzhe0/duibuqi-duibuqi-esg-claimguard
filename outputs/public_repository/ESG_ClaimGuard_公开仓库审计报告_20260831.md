# ESG ClaimGuard 公开仓库审计报告

> 审计日期：2026-08-31  
> 目标：形成可上传至赛事或代码托管网站的干净、可复验仓库快照。

## 结论

公开仓库快照已经生成并通过独立解压复验，可以作为代码与轻量数据材料上传。不要直接上传当前工作目录，也不要上传或推送当前 `.git` 历史。

- 公开 ZIP：`outputs/public_repository/ESG_ClaimGuard_公开代码仓库.zip`
- 文件数：168（含 `PUBLIC_REPOSITORY_MANIFEST.json`）
- 大小：13.168 MiB
- SHA-256：`5e34cd1876a91ea0deade0fff34c6129d10d964cc7139bc788c9dc15fb4ad856`
- ZIP CRC：通过
- 敏感信息与个人绝对路径扫描：通过
- 200 MiB 限制：通过

## 原始仓库风险

当前工作目录约 15 GiB，`.git` 历史约 4.8 GiB；版本状态中还有约 43,911 个历史删除路径。原目录同时包含原始 ESG 报告、MinerU parsed 产物、运行日志、前端与视频依赖缓存等大体积资产。因此，直接压缩工作目录或复用现有 Git 历史都不适合作为公开交付。

本轮没有删除或重写原 Git 历史，也没有擅自清理用户数据。公开交付通过白名单脚本在独立 ZIP 中完成。

## 公开快照包含

- `dashboard_api/`、`dashboard_web/`、`src/`、`scripts/` 和 `tests/`；
- 项目说明、提交文档源稿、XeLaTeX 源文件与正式图表；
- 正式运行的轻量 manifest、抽取结果、验证报告与校验和；
- Remotion 视频工程源代码及项目演示素材；
- 两份正式 PDF、最终核验记录和逐文件 SHA-256 manifest；
- `.env.example`、增强后的 `.gitignore`、第三方与再分发 `NOTICE.md`。

## 明确排除

- `.git` 历史；
- `data/raw_pdfs/` 和完整 `parsed/`；
- 模型权重、运行日志和本机软链接；
- `node_modules`、构建目录和缓存；
- `.env`、密钥、令牌、私钥与个人绝对路径；
- 最终 MP4 和独立 WAV，它们仍作为比赛正式文件单独管理。

## 已完成修正

1. 正式材料删除“下一阶段完成 300 条 Natural-Gold”“双人标注、第三人仲裁”“0/300”等未完成式表述。
2. 统一为：本作品声明工程完整性与证据可追溯性，不声明未经独立人工评测的 Precision、Recall 或 F1。
3. 正式网页移除未完成式金标准导航入口，质量页改为展示已完成的工程验收边界。
4. 文档统一由 XeLaTeX 生成 PDF，图片和表格越界会使构建失败。
5. 修复公开快照缺少前端 `index.html` 和轻量数据台账的问题。
6. 将 `pdfjs-dist` 固定到无当前公告漏洞的兼容版本，并更新传递依赖；`npm audit` 为 0 项漏洞。
7. 增加公开快照白名单、路径安全、敏感内容、大小、CRC 和逐文件 SHA-256 检查。
8. 将旧的 300 条 Natural-Gold 建设协议从正式辅助材料和公开快照中排除，避免把本次交付范围之外的可选评测计划误写成未完成任务。

## 独立解压复验

在 `/tmp/esg-public-review-20260831-v4` 独立解压后执行：

- Python：68 项测试通过，6 项按设计跳过。跳过项需要完整 parsed 证据或未纳入本次作品验收的独立人工评测夹具；
- Dashboard：`npm ci` 成功；`npm audit --audit-level=moderate` 为 0 项漏洞；生产构建成功；
- 原工作区生产 smoke：18 项通过；
- Remotion：TypeScript 无错误；
- 正式比赛文件：4/4，SHA-256 全部通过。

## 上传方式

赛事或普通材料网站可直接上传公开 ZIP。若上传至 GitHub、GitLab、Gitee 等 Git 托管网站：

1. 解压公开 ZIP 到一个新目录；
2. 不复制原 `.git`；
3. 在新目录执行 `git init`；
4. 检查 `PUBLIC_REPOSITORY_MANIFEST.json` 后再首次提交。

当前 `NOTICE.md` 只声明赛事评审与第三方边界，没有向整个项目授予统一开源许可证。如果网站要求开源许可证，需要权利人另行选择并加入 `LICENSE`；这不影响赛事材料上传。

## 视频说明

根据“先提交现有内容、不再新增制作任务”的要求，本轮没有重新渲染最终 MP4。仓库源代码中的准确率边界文案已改为最终口径，正式视频文件保持此前已验收版本。
